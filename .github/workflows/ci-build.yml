name: Node.js

on:
  push:
    branches:
      - main
  pull_request:

env:
  LATEST_SUPPORTED_NODE: "24.x"

jobs:
  test:
    timeout-minutes: 6
    strategy:
      fail-fast: false
      matrix:
        os:
          - "ubuntu-latest"
          - "windows-latest"
        node-version:
          - "20.x"
          - "22.x"
          - "24.x"
          - "26.x"
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    steps:
      - name: Configure git settings (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          git config --global core.autocrlf false
          git config --global core.eol lf
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node-version }}
      - name: Check versions
        run: |
          node --version
          npm --version
      - name: Install dependencies
        run: npm ci --verbose
      - name: Build packages
        run: npm run build
      - name: Lint
        run: npm run lint
      - name: Build docs
        if: matrix.node-version == env.LATEST_SUPPORTED_NODE
        run: npm run docs
      - name: Run tests
        if: matrix.node-version != env.LATEST_SUPPORTED_NODE
        run: npm test
      - name: Run test coverage
        if: matrix.node-version == env.LATEST_SUPPORTED_NODE
        run: npm run test:coverage
      - name: Upload code coverage
        if: matrix.node-version == env.LATEST_SUPPORTED_NODE && matrix.os == 'ubuntu-latest'
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          fail_ci_if_error: true
          files: packages/*/lcov.info
          flags: cli-hooks,cli-test,logger,oauth,socket-mode,web-api,webhook
          report_type: coverage
          token: ${{ secrets.CODECOV_TOKEN }}
          verbose: true
      - name: Upload test results
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          fail_ci_if_error: true
          files: packages/cli-hooks/test-results.xml,packages/cli-test/test-results.xml,packages/logger/test-results.xml,packages/oauth/test-results.xml,packages/socket-mode/test-results.xml,packages/web-api/test-results.xml,packages/webhook/test-results.xml
          flags: ${{ matrix.node-version }},${{ matrix.os }}
          report_type: test_results
          token: ${{ secrets.CODECOV_TOKEN }}
          verbose: true
      # The lockfile pins undici v7; re-run socket-mode against the v8 peer range, which needs Node >=22.19.
      # These run after the uploads so they don't overwrite socket-mode's v7 test-results.xml.
      - name: Install undici v8 for socket-mode
        if: matrix.node-version != '20.x'
        run: npm install undici@^8 --workspace=packages/socket-mode --no-save
      - name: Check undici version
        if: matrix.node-version != '20.x'
        working-directory: packages/socket-mode
        run: node -e "const v = require('undici/package.json').version; console.log('undici', v); if (!v.startsWith('8.')) process.exit(1);"
      - name: Run socket-mode tests (undici v8)
        if: matrix.node-version != '20.x'
        run: npm test --workspace=packages/socket-mode
