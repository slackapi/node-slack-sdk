{
  "scripts": {
    "test": "lerna run test",
    "lint": "lerna run lint",
    "ref-docs": "lerna run ref-docs:model --no-bail && lerna run start --scope @slack/sdk-ref-docs"
  },
  "devDependencies": {
    "lerna": "^3.13.1"
  }
}
